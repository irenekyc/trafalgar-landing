import styled from "styled-components";
import { themeData } from "../../theme";

const H5 = styled.h5`
  font-size: 18px;
  font-weight: 300;
  line-height: 30px;
  color: ${themeData.colors.light};
`;

export default H5;
