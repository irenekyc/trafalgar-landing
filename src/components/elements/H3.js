import styled from "styled-components";
import { themeData } from "../../theme";

const H3 = styled.h3`
  font-size: 21px;
  line-height: 32px;
  font-weight: 300;
  color: ${themeData.colors.light};
`;

export default H3;
